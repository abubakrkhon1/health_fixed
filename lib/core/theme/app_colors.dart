import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF277DFF);
  static const dark = Color(0xFF0F1A0F);
  static const background = Colors.white;
  static const bottomNavBackground = Color.fromARGB(255, 248, 248, 248);
  static const textPrimary = Colors.black;
  static const textSecondary = Colors.grey;
  static const danger = Colors.redAccent;

  // You can also group them:
  static const medicationCard = Color(0xFFE0F5E9);
}